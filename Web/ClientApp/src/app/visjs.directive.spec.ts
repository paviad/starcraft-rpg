import { VisjsDirective } from './visjs.directive';

describe('VisjsDirective', () => {
  it('should create an instance', () => {
    const directive = new VisjsDirective();
    expect(directive).toBeTruthy();
  });
});
